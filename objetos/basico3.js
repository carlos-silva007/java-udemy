const usuario = {
    email: 'carlos@itau.com.br'
};
//para atribuir um nome ao objeto usuario
usuario.nome = 'Carlos'
usuario.senha = '123456'

console.log(usuario);
console.log(`${usuario.nome}/${usuario.senha}/${usuario.email}`);
